{
  "name": "filebaser",
  "description": "Database simulator based on MongoDB",
  "version": "0.7.13",
  "dependencies": {
    "uniqid": "^4.1.1"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.2.0"
  },
  "keywords": [
    "database",
    "mongodb",
    "mongoose"
  ],
  "license": "MIT",
  "main": "index.php",
  "repository": "vinyguedess/filebaser",
  "scripts": {
    "precommit": "lint-staged",
    "test": "node ./node_modules/.bin/_mocha --reporter spec --ui tdd ./tests/"
  },
  "lint-staged": {
    "*.js": [
      "prettier --write",
      "git add"
    ]
  }
}
